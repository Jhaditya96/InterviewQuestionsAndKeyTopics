
>Unlike innerText, though, innerHTML lets you work with HTML rich text and does not automatically encode and decode text. 
 In other words, innerText retrieves and sets the content of the tag as plain text, whereas innerHTML retrieves and sets the content
 in HTML format.